fun main(args: Array<String>) {
    val niceJob = 1.0

    val nn = 1L
    val n: Long = 1
    fun family(firstName: String, surname: String) {

        (0.rangeTo(100) step (5)).forEach() { i: Int ->
        }

        for (i in 0.rangeTo(100) step (5)) {
        }
        (0..100).forEach { number: Int ->

        }
        println()

    }

    val k: Char = 'j'
    var h: Int
    h = 5
    h = 7
    fun drive(): String? {
        return null
    }

    val car: String? = "samand"
    // car?.drive()

    val drive: String? = drive()

    println("$h \n $nn ")

    val r = 3
    val circle = PI * r.toDouble().pow(2)
    println(circle)
    //  var f:Int=null
    var numOfClass: Int? = null
//    if (numOfClass!!>5){
    // }
    val m = false
    println(!m)

    var name = "ati"
    if (name!! == "ati") {
        println("sisi")
    } else if (name == "nazi") {
        println("me")
    } else {
        println("un known")
    }

    when (name) {
        "ati" -> println("sisi")
        "nazi" -> println("me")
        else -> {
            println("un known")
        }
    }
    var numbers = mutableListOf(4, 17, 15)
    println(numbers[2])
    println(" *${numbers.get(2)}*")
    val score = 4
    numbers[1] = score
    println(numbers)
    numbers.set(2, score)
    println("****${numbers[2]}")
    var x = 4
    do {
        x--
    } while (x > 0)


}